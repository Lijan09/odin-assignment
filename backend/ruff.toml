line-length = 100
target-version = "py311"

[lint]
# E/F  pycodestyle + pyflakes   I  import order      B  likely bugs
# UP   modern syntax            S  security (flake8-bandit)
select = ["E", "F", "I", "B", "UP", "S"]

[lint.per-file-ignores]
# assert is the point of a test
"tests/**" = ["S101"]
